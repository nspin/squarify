from setuptools import setup, find_packages

setup(
    name='squarify',
    version='0.1.0.0',
    packages=['squarify'],
    package_data={
        'squarify': ['resources/index.html'],
        }
    )
